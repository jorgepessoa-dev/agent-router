{
  "name": "claudecode-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Transparent router for Claude Code: routes requests across MiniMax, Haiku and Opus.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
